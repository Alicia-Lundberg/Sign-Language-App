import { Text, View } from 'react-native'

export default function DragAndDropQuestion({ current }) {
  return (
    <View style={{ marginTop: 30 }}>
      <Text style={{ fontSize: 18, textAlign: 'center' }}>
        Dra rätt ord till rätt plats (kommer snart)
      </Text>
      {/* Här lägger du in din framtida drag-och-släpp-logik */}
    </View>
  )
}
